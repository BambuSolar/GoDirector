{
  "name": "application-name",
  "version": "0.0.1",
  "scripts": {
    "mocks": "json-server db.json --port 5000 --routes routes.json --delay 1000"
  },
  "devDependencies": {
    "json-server": "^0.9.6"
  }
}
